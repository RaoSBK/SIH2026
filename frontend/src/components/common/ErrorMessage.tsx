import React from 'react';

export interface ErrorMessageProps {
  // Add props here
}

export const ErrorMessage: React.FC<ErrorMessageProps> = () => {
  return (
    <div>
      <h3>ErrorMessage Component</h3>
    </div>
  );
};

export default ErrorMessage;
