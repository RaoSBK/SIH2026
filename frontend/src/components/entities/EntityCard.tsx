import React from 'react';

export interface EntityCardProps {
  // Add props here
}

export const EntityCard: React.FC<EntityCardProps> = () => {
  return (
    <div>
      <h3>EntityCard Component</h3>
    </div>
  );
};

export default EntityCard;
