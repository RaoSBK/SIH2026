[tool.poetry]
name = "criminal-intelligence-platform"
version = "0.1.0"
description = "Criminal Intelligence Platform"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.110.0"
uvicorn = "^0.28.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
